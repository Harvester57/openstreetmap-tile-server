version: '3'

services:
  map:
    image: ghcr.io/harvester57/openstreetmap-tile-server:master
    volumes:
      - osm-data:/data/database/
    ports:
      - "8080:80"
    command: "run"

volumes:
  osm-data:
    external: true
